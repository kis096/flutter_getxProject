import 'dart:ui';

const purple = Color(0xFF756BFC);
const pink = Color(0xFFFF1A39A);  // Added missing 'FF' for full opacity
const deepPink = Color(0xFFFFA63C6);  // Added missing 'FF' for full opacity
const green = Color(0xFF41CF9F);
const yellow = Color(0xFFEEC38E);
const lightBlue = Color(0xFF42A5F5);  // Fixed typo from 'ligthBlue'
const blue = Color(0xFF2B60E6);
